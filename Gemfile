source "https://rubygems.org"

# Use the github-pages gem so the local / CI build uses exactly the same
# Jekyll version and whitelisted plugin set as GitHub Pages itself.
gem "github-pages", group: :jekyll_plugins